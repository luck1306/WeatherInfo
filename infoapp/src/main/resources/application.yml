spring:
    application:
        name: infoapp
    datasource:
        username: ${USERNAME}
        password: ${PASSWORD}
        url: ${URL}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: validate
        generate-ddl: false